version: '3.8'
services:
  aethra:
    image: aethra:latest
    build: ../
    environment:
      - PORT=3000
      - P2P_PORT=7100
      - HOST=0.0.0.0
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    networks:
      - aethra-net

networks:
  aethra-net:
    driver: bridge
